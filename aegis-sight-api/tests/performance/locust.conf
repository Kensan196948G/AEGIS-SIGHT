# ===========================================================================
# AEGIS-SIGHT -- Locust configuration
# ===========================================================================
# Override any value via CLI flags or environment variables.
#   locust -f tests/performance/locustfile.py --config tests/performance/locust.conf

locustfile = tests/performance/locustfile.py
host        = http://localhost:8000
users       = 100
spawn-rate  = 10
run-time    = 5m
headless    = true
only-summary = true
